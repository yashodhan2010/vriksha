# Root project environment notes.
#
# The active app is in web/. Copy web/.env.example to web/.env.local for local development.
# The old Python trading implementation has been moved to legacy_trading_platform/.

ACTIVE_APP=web
